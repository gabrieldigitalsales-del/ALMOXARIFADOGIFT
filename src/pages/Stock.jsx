import{useMemo,useState}from'react';import{FileDown,Plus,Search,ShoppingCart,ArrowDownUp,QrCode,Printer}from'lucide-react';import QRCode from'qrcode';import PageHeader from'../components/PageHeader';import DataTable from'../components/DataTable';import Modal from'../components/Modal';import FormGrid,{Field}from'../components/FormGrid';import{useApp}from'../context/AppContext';import{categories}from'../data/seed';import{currency,statusOf}from'../utils/costs';import{exportExcel,exportPDF}from'../utils/exporters';import{classifyItem,applyItemClassification}from'../utils/itemClassifier';
const applications=['Produção de máquinas','Estoque geral','Manutenção','Consumível','Ferramenta','EPI'];
const blank={code:'',name:'',category:'Matéria-prima',subcategory:'Chapas',qty:0,reserved:0,unit:'un',unitCost:0,avgCost:0,supplier:'',min:1,leadTime:0,application:'Produção de máquinas',status:'OK'};
const machineNames=(productId,bom,machines)=>{const ids=[...new Set(bom.filter(b=>b.productId===productId).map(b=>b.machineId))];return ids.map(id=>machines.find(m=>m.id===id)?.name).filter(Boolean)};
export default function Stock(){const{stock,upsertStock,deleteStock,quickMove,notify,createPurchaseSuggestion,movements}=useApp();const[query,setQuery]=useState('');const[cat,setCat]=useState('Todos');const[status,setStatus]=useState('Todos');const[edit,setEdit]=useState(null);const[move,setMove]=useState(null);const[view,setView]=useState(null);const[qr,setQr]=useState(null);const rows=useMemo(()=>stock.filter(i=>(cat==='Todos'||i.category===cat)&&(status==='Todos'||statusOf(i)===status)&&(`${i.name} ${i.code} ${i.category} ${i.supplier} ${i.application||''}`).toLowerCase().includes(query.toLowerCase())),[stock,query,cat,status]);const save=()=>{const smart=classifyItem(edit?.name||'',stock,edit?.id);upsertStock({...edit,code:edit?.code||smart.code,category:edit?.category||smart.category,subcategory:edit?.subcategory||smart.subcategory,unit:edit?.unit||smart.unit,avgCost:edit.avgCost||edit.unitCost,application:edit.application||smart.application||'Produção de máquinas'});setEdit(null);notify('Item salvo')};const smartEdit=edit?classifyItem(edit?.name||'',stock,edit?.id):null;const smartNameChange=v=>setEdit(applyItemClassification({...edit,name:v},stock,edit?.id));const genQr=async r=>{setQr({item:r,url:await QRCode.toDataURL(`${location.origin}/produto/${r.code}`)})};const printLabel=()=>window.print();const cols=[{key:'code',label:'Código'},{key:'name',label:'Item'},{key:'application',label:'Uso',render:r=>r.application||'Produção de máquinas'},{key:'qty',label:'Estoque atual'},{key:'avgCost',label:'Custo médio',render:r=>currency(r.avgCost||r.unitCost)},{key:'status',label:'Status',render:r=><div className="min-w-[120px]"><span className={`badge ${statusOf(r)==='OK'?'bg-green-100 text-green-700':statusOf(r)==='Estoque baixo'?'bg-red-100 text-brand-red':'bg-brand-red text-white'}`}>{statusOf(r)}</span></div>},{key:'actions2',label:'Operações',render:r=><div className="flex flex-wrap gap-2"><button className="btn-ghost p-2" title="Entrada/Saída" onClick={()=>setMove({productId:r.id,type:'entrada',qty:1,reason:''})}><ArrowDownUp size={15}/></button><button className="btn-ghost p-2" title="Sugerir compra" onClick={()=>createPurchaseSuggestion(r)}><ShoppingCart size={15}/></button><button className="btn-ghost p-2" title="QR Code" onClick={()=>genQr(r)}><QrCode size={15}/></button></div>}];return <><PageHeader title="Estoque" subtitle="Cadastro minimalista dos produtos e materiais disponíveis" actions={<><button onClick={()=>exportPDF('Estoque',rows)} className="btn-ghost"><FileDown size={18}/>PDF</button><button onClick={()=>exportExcel('Estoque',rows)} className="btn-ghost"><FileDown size={18}/>Excel</button><button onClick={()=>setEdit(blank)} className="btn-primary"><Plus size={18}/>Adicionar</button></>}/><div className="card mb-4 grid gap-3 md:grid-cols-4"><label className="flex items-center gap-3 md:col-span-2"><Search size={18}/><input className="flex-1 bg-transparent outline-none" placeholder="Buscar por código, produto, fornecedor ou uso..." value={query} onChange={e=>setQuery(e.target.value)}/></label><select className="input" value={cat} onChange={e=>setCat(e.target.value)}><option>Todos</option>{Object.keys(categories).map(c=><option key={c}>{c}</option>)}</select><select className="input" value={status} onChange={e=>setStatus(e.target.value)}><option>Todos</option><option>OK</option><option>Estoque baixo</option><option>Em falta</option></select></div><DataTable columns={cols} rows={rows} onView={setView} onEdit={setEdit} onDelete={r=>deleteStock(r.id)}/><Modal open={!!edit} title="Cadastro de item" onClose={()=>setEdit(null)}><FormGrid><div><label className="mb-1 block text-xs font-semibold uppercase text-brand-steel">Código automático</label><div className="input bg-brand-light font-mono text-brand-steel">{edit?.code||smartEdit?.code||'Digite o nome'}</div></div><Field label="Nome" value={edit?.name} onChange={smartNameChange}/>{edit?.name&&<div className="border border-brand-line bg-brand-light p-3 text-sm text-brand-steel md:col-span-2 dark:border-white/10 dark:bg-white/5 dark:text-white/70"><p className="font-semibold text-brand-black dark:text-white">Classificação automática</p><p>Categoria: <b>{smartEdit?.category}</b> / {smartEdit?.subcategory}</p><p>Uso: <b>{smartEdit?.application}</b> • Unidade: <b>{smartEdit?.unit}</b> • Código: <b className="font-mono">{smartEdit?.code}</b></p><p className="text-xs">Regra detectada: {smartEdit?.matchedRule}</p></div>}<Field label="Uso do item" value={edit?.application||'Produção de máquinas'} options={applications} onChange={v=>setEdit({...edit,application:v})}/><Field label="Categoria" value={edit?.category} options={Object.keys(categories)} onChange={v=>setEdit({...edit,category:v,subcategory:categories[v][0]})}/><Field label="Subcategoria" value={edit?.subcategory} options={categories[edit?.category||'Matéria-prima']} onChange={v=>setEdit({...edit,subcategory:v})}/><Field label="Estoque atual" type="number" min="0" step="1" value={edit?.qty} onChange={v=>setEdit({...edit,qty:v})}/><Field label="Unidade" value={edit?.unit} onChange={v=>setEdit({...edit,unit:v})}/><Field label="Valor unitário" type="number" min="0" step="0.01" value={edit?.unitCost} onChange={v=>setEdit({...edit,unitCost:v})}/><Field label="Preço médio" type="number" min="0" step="0.01" value={edit?.avgCost} onChange={v=>setEdit({...edit,avgCost:v})}/><Field label="Estoque mínimo" type="number" min="1" step="1" value={edit?.min} onChange={v=>setEdit({...edit,min:v})}/><Field label="Fornecedor" value={edit?.supplier} onChange={v=>setEdit({...edit,supplier:v})}/></FormGrid><button className="btn-primary mt-5" onClick={save}>Salvar item</button></Modal><Modal open={!!move} title="Entrada / Saída rápida" onClose={()=>setMove(null)}><FormGrid><Field label="Tipo" value={move?.type} options={['entrada','saída','devolução','perda','transferência','reserva produção']} onChange={v=>setMove({...move,type:v})}/><Field label="Quantidade" type="number" min="1" step="1" value={move?.qty} onChange={v=>setMove({...move,qty:v})}/><Field label="Motivo" value={move?.reason} onChange={v=>setMove({...move,reason:v})}/></FormGrid><button className="btn-primary mt-5" onClick={()=>{quickMove(move);setMove(null)}}>Registrar movimentação</button></Modal><Modal open={!!view} title="Detalhes do item" onClose={()=>setView(null)} size="max-w-5xl"><div className="mb-4 grid gap-3 md:grid-cols-4"><div className="card"><b>Estoque atual</b><p>{view?.qty}</p></div><div className="card"><b>Estoque mínimo</b><p>{view?.min}</p></div><div className="card"><b>Status</b><p>{view&&statusOf(view)}</p></div><div className="card"><b>Valor</b><p>{view&&currency(view.qty*(view.avgCost||view.unitCost))}</p></div></div><DataTable rows={movements.filter(m=>m.item===view?.name)} columns={[{key:'date',label:'Data'},{key:'type',label:'Tipo'},{key:'qty',label:'Qtd'},{key:'reason',label:'Motivo'},{key:'op',label:'OP'}]}/></Modal><Modal open={!!qr} title="QR Code e etiqueta" onClose={()=>setQr(null)}><div className="grid gap-6 md:grid-cols-[220px,1fr]"><div className="border p-4"><img src={qr?.url}/></div><div><h2 className="text-2xl font-semibold">{qr?.item.name}</h2><p className="font-mono">{qr?.item.code}</p><p>{qr?.item.application||'Estoque geral'}</p><p>{qr?.item.category} / {qr?.item.subcategory}</p><button className="btn-warning mt-5" onClick={printLabel}><Printer size={18}/>Imprimir etiqueta</button></div></div></Modal></>}
