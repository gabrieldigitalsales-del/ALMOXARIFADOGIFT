import React, { useEffect, useMemo, useState } from 'react';
import { createClient } from '@supabase/supabase-js';
import { Building2, Truck, ClipboardList, History, Settings, Plus, Send, Search, Package, FileText, Download, Lock, LogOut, CheckCircle2, Pencil, Trash2, Save, X, Database, Menu, Copy, MessageCircle, ArrowRight, RotateCcw, Star, Timer, DollarSign, Upload, CheckSquare, Square, ExternalLink } from 'lucide-react';

const FIXED_DESTINATION = {
  company: 'GIFT EXCELLENCE', cnpj: '48.969.022/0001-90', cep: '35701-590',
  address: 'Rua Geraldino Figueiredo, nº 373 - Brejão, Sete Lagoas/MG',
  phone: '31 99836-0241', responsible: 'Gabriel', hours: '07h às 17h'
};
const PASSWORD = 'asd123';
const DEFAULT_CARRIERS = [
  { id:'car_sete_lagos', name:'Sete Lagos Transportes', whatsapp:'', email:'', city:'Sete Lagoas/MG', cargo_type:'Carga fracionada e fechada', quote_mode:'Manual / site', api_status:'Sem API pública confirmada', website:'https://setelagos.com.br/contato/', notes:'Confirmar WhatsApp da unidade que atende a GIFT.', active:true, created_at:'2026-06-22T00:00:00.000Z' },
  { id:'car_jadlog', name:'Jadlog', whatsapp:'', email:'', city:'Nacional', cargo_type:'Encomendas e carga fracionada', quote_mode:'Site / API futura', api_status:'API sob contrato/credencial', website:'https://www.jadlog.com.br/atendimento', notes:'Atendimento pelo site/Jade. Preencher WhatsApp comercial da unidade se houver.', active:true, created_at:'2026-06-22T00:00:00.000Z' },
  { id:'car_braspress', name:'Braspress', whatsapp:'1122233500', email:'sac@braspress.com', city:'Nacional', cargo_type:'Carga fracionada urgente', quote_mode:'WhatsApp / site', api_status:'API futura/contrato', website:'https://www.braspress.com/contato/', notes:'WhatsApp público da assistente virtual Romilda. Confirmar uso comercial.', active:true, created_at:'2026-06-22T00:00:00.000Z' },
  { id:'car_rodonaves', name:'Rodonaves / RTE', whatsapp:'', email:'', city:'Nacional', cargo_type:'Carga fracionada e logística', quote_mode:'Site / unidade', api_status:'API futura/portal do cliente', website:'https://rodonaves.com.br/fale-conosco.html', notes:'Confirmar unidade e WhatsApp comercial.', active:true, created_at:'2026-06-22T00:00:00.000Z' },
  { id:'car_patrus', name:'Patrus Transportes', whatsapp:'', email:'', city:'Nacional', cargo_type:'Carga fracionada', quote_mode:'Manual / site', api_status:'API futura/contrato', website:'', notes:'Preencher contato da unidade que atende Sete Lagoas/MG.', active:true, created_at:'2026-06-22T00:00:00.000Z' },
  { id:'car_jamef', name:'Jamef', whatsapp:'', email:'', city:'Nacional', cargo_type:'Carga fracionada', quote_mode:'Manual / site', api_status:'API futura/contrato', website:'', notes:'Preencher contato comercial da unidade.', active:true, created_at:'2026-06-22T00:00:00.000Z' },
  { id:'car_azul', name:'Azul Cargo Express', whatsapp:'', email:'', city:'Aéreo / nacional', cargo_type:'Carga aérea e encomendas', quote_mode:'Site / API futura', api_status:'API futura/contrato', website:'', notes:'Boa opção para volumes menores e urgentes.', active:true, created_at:'2026-06-22T00:00:00.000Z' },
  { id:'car_latam', name:'LATAM Cargo', whatsapp:'', email:'', city:'Aéreo / nacional', cargo_type:'Carga aérea', quote_mode:'Site / API futura', api_status:'API futura/contrato', website:'', notes:'Boa opção para cargas urgentes.', active:true, created_at:'2026-06-22T00:00:00.000Z' },
  { id:'car_fedex_tnt', name:'FedEx / TNT', whatsapp:'', email:'', city:'Nacional / internacional', cargo_type:'Expressa e carga fracionada', quote_mode:'Site / API futura', api_status:'API futura/contrato', website:'', notes:'Preencher contato comercial se houver contrato.', active:true, created_at:'2026-06-22T00:00:00.000Z' },
  { id:'car_loggi', name:'Loggi', whatsapp:'', email:'', city:'Nacional', cargo_type:'Encomendas', quote_mode:'Site / API futura', api_status:'API futura/contrato', website:'', notes:'Mais indicada para encomendas; validar carga industrial.', active:true, created_at:'2026-06-22T00:00:00.000Z' },
  { id:'car_correios', name:'Correios', whatsapp:'', email:'', city:'Nacional', cargo_type:'Encomendas', quote_mode:'Site / API futura', api_status:'API futura/contrato', website:'', notes:'Usar para volumes pequenos dentro dos limites dos Correios.', active:true, created_at:'2026-06-22T00:00:00.000Z' },
  { id:'car_buslog', name:'Buslog', whatsapp:'', email:'', city:'Nacional', cargo_type:'Encomendas rodoviárias', quote_mode:'Manual / site', api_status:'API futura/contrato', website:'', notes:'Validar cobertura para Sete Lagoas/MG.', active:true, created_at:'2026-06-22T00:00:00.000Z' },
  { id:'car_translovato', name:'Translovato', whatsapp:'', email:'', city:'Nacional', cargo_type:'Carga fracionada', quote_mode:'Manual / site', api_status:'API futura/contrato', website:'', notes:'Preencher contato comercial da unidade.', active:true, created_at:'2026-06-22T00:00:00.000Z' },
  { id:'car_sao_miguel', name:'Expresso São Miguel', whatsapp:'', email:'', city:'Nacional', cargo_type:'Carga fracionada', quote_mode:'Manual / site', api_status:'API futura/contrato', website:'', notes:'Preencher contato comercial da unidade.', active:true, created_at:'2026-06-22T00:00:00.000Z' },
  { id:'car_total_express', name:'Total Express', whatsapp:'', email:'', city:'Nacional', cargo_type:'Encomendas / e-commerce', quote_mode:'Site / API futura', api_status:'API futura/contrato', website:'', notes:'Mais indicada para encomendas/e-commerce.', active:true, created_at:'2026-06-22T00:00:00.000Z' }
];
const STORAGE_KEYS = { auth:'gift_log_auth', suppliers:'gift_log_suppliers', carriers:'gift_log_carriers', quotes:'gift_log_quotes', destination:'gift_log_destination' };
const supabaseUrl = import.meta.env.VITE_SUPABASE_URL;
const supabaseAnon = import.meta.env.VITE_SUPABASE_ANON_KEY;
const supabase = supabaseUrl && supabaseAnon ? createClient(supabaseUrl, supabaseAnon) : null;
const usingSupabase = Boolean(supabase);
function safeRead(key, fallback){ try{return JSON.parse(localStorage.getItem(key)) ?? fallback}catch{return fallback} }
function safeWrite(key, value){ localStorage.setItem(key, JSON.stringify(value)); }
function uid(prefix='id'){ return `${prefix}_${Date.now()}_${Math.random().toString(16).slice(2)}`; }
function brl(value){ const n=Number(String(value||'').replace(',','.')); return Number.isFinite(n)?n.toLocaleString('pt-BR',{style:'currency',currency:'BRL'}):'R$ 0,00'; }
function onlyDigits(v=''){ return String(v).replace(/\D/g,''); }
function encodeWhats(text){ return encodeURIComponent(text).replace(/%20/g,'+'); }
function formatDate(d){ return new Date(d).toLocaleString('pt-BR',{dateStyle:'short',timeStyle:'short'}); }
function calcCubage({length,width,height,volumes}){ const l=Number(String(length||'').replace(',','.')); const w=Number(String(width||'').replace(',','.')); const h=Number(String(height||'').replace(',','.')); const v=Number(String(volumes||1).replace(',','.'))||1; if(!l||!w||!h) return 0; return ((l*w*h)/1000000)*v; }
async function dbSelect(table, fallbackKey){ if(!supabase) return safeRead(fallbackKey,[]); const {data,error}=await supabase.from(table).select('*').order('created_at',{ascending:false}); return error?safeRead(fallbackKey,[]):(data||[]); }
async function dbUpsert(table, fallbackKey, rows){ safeWrite(fallbackKey, rows); if(!supabase) return; await supabase.from(table).delete().neq('id','never-match-this-id'); if(rows.length) await supabase.from(table).insert(rows); }
function Field({label,value,onChange,placeholder,type='text',textarea=false,disabled=false}){ return <label className="field"><span>{label}</span>{textarea?<textarea disabled={disabled} value={value||''} placeholder={placeholder} onChange={e=>onChange(e.target.value)}/>:<input disabled={disabled} type={type} value={value||''} placeholder={placeholder} onChange={e=>onChange(e.target.value)}/>}</label>; }
function SelectField({label,value,onChange,children}){ return <label className="field"><span>{label}</span><select value={value||''} onChange={e=>onChange(e.target.value)}>{children}</select></label>; }
function Toggle({label,checked,onChange}){ return <button className={`toggle ${checked?'on':''}`} type="button" onClick={()=>onChange(!checked)}><span></span>{label}</button>; }
function Empty({text}){return <div className="empty"><Package size={30}/><p>{text}</p></div>}
function Stat({label,value}){return <div className="stat"><span>{label}</span><strong>{value}</strong></div>}
function stageLabel(s){ return ({rascunho:'Rascunho',enviada:'Enviada',aguardando:'Aguardando',parcial:'Parcial',respondido:'Respondida',fechado:'Fechada',cancelado:'Cancelada'}[s]||s||'Rascunho'); }

export default function FreightModule(){
 const auth=true; const [screen,setScreen]=useState('dashboard'); const [mobileMenu,setMobileMenu]=useState(false);
 const [destination,setDestinationState]=useState(safeRead(STORAGE_KEYS.destination,FIXED_DESTINATION)); const [suppliers,setSuppliers]=useState([]); const [carriers,setCarriers]=useState([]); const [quotes,setQuotes]=useState([]);
 useEffect(()=>{ loadAll(); },[]);
 async function loadAll(){ setSuppliers(await dbSelect('gift_log_suppliers', STORAGE_KEYS.suppliers)); const loaded=await dbSelect('gift_log_carriers', STORAGE_KEYS.carriers); if(!loaded.length){setCarriers(DEFAULT_CARRIERS); safeWrite(STORAGE_KEYS.carriers,DEFAULT_CARRIERS)}else setCarriers(loaded); setQuotes(await dbSelect('gift_log_quotes', STORAGE_KEYS.quotes)); }
 async function syncSuppliers(rows){ setSuppliers(rows); await dbUpsert('gift_log_suppliers', STORAGE_KEYS.suppliers, rows); }
 async function syncCarriers(rows){ setCarriers(rows); await dbUpsert('gift_log_carriers', STORAGE_KEYS.carriers, rows); }
 async function syncQuotes(rows){ setQuotes(rows); await dbUpsert('gift_log_quotes', STORAGE_KEYS.quotes, rows); }
 function setDestination(v){ setDestinationState(v); safeWrite(STORAGE_KEYS.destination,v); }

 const content = {dashboard:<Dashboard setScreen={setScreen} destination={destination} suppliers={suppliers} carriers={carriers} quotes={quotes}/>, quote:<QuoteForm destination={destination} suppliers={suppliers} carriers={carriers} setSuppliers={syncSuppliers} quotes={quotes} setQuotes={syncQuotes} setScreen={setScreen}/>, suppliers:<Suppliers suppliers={suppliers} setSuppliers={syncSuppliers}/>, carriers:<Carriers carriers={carriers} setCarriers={syncCarriers}/>, history:<HistoryPage quotes={quotes} setQuotes={syncQuotes} carriers={carriers}/>, settings:<SettingsPage destination={destination} setDestination={setDestination}/>}[screen];
 return <div className="gift-freight-module"><div className="moduleHeader"><div><h1>{titleFor(screen)}</h1><p>Controle completo de fretes, coletas, fornecedores e transportadoras.</p></div><div className="dbtag"><Database size={15}/>{usingSupabase?'Supabase conectado':'Modo local / Supabase ready'}</div></div><Nav active={screen} setScreen={(value)=>setScreen(value)}/><div className="moduleContent">{content}</div></div>;
}
function Brand(){return <div className="brand"><img src="/logo-gift.png"/><div><strong>GIFT LOG</strong><small>Fretes & Coletas</small></div></div>}
function Login({setAuth}){const [pass,setPass]=useState(''); const [err,setErr]=useState(''); function submit(e){e.preventDefault(); if(pass===PASSWORD){safeWrite(STORAGE_KEYS.auth,true); setAuth(true)} else setErr('Senha incorreta.')} return <div className="login"><form className="loginCard" onSubmit={submit}><img src="/logo-gift.png"/><span className="pill"><Lock size={14}/> Acesso interno</span><h1>GIFT LOG</h1><p>Sistema de cotações, fila de WhatsApp e controle de respostas de transportadoras.</p>{err&&<div className="err">{err}</div>}<Field label="Senha" type="password" value={pass} onChange={setPass} placeholder="Digite a senha"/><button className="primary full">Entrar</button><small>Senha padrão: asd123</small></form></div>}
function titleFor(s){return ({dashboard:'Dashboard',quote:'Nova cotação',suppliers:'Fornecedores',carriers:'Transportadoras',history:'Histórico',settings:'Configurações'}[s])}
function Nav({active,setScreen}){const items=[['dashboard',ClipboardList,'Dashboard'],['quote',Send,'Nova cotação'],['suppliers',Building2,'Fornecedores'],['carriers',Truck,'Transportadoras'],['history',History,'Histórico'],['settings',Settings,'Configurações']]; return <nav>{items.map(([id,Icon,label])=><button key={id} className={active===id?'active':''} onClick={()=>setScreen(id)}><Icon size={18}/>{label}</button>)}</nav>}
function Dashboard({setScreen,destination,suppliers,carriers,quotes}){
 const active=carriers.filter(c=>c.active!==false).length; const pending=quotes.filter(q=>['enviada','aguardando','parcial'].includes(q.status)).length; const closed=quotes.filter(q=>q.status==='fechado').length;
 return <section className="gridPage"><div className="cards4"><Stat label="Cotações" value={quotes.length}/><Stat label="Aguardando" value={pending}/><Stat label="Transportadoras" value={active}/><Stat label="Fechadas" value={closed}/></div><div className="panel destination"><div className="sectionTitle"><Building2/>Destino fixo</div><h2>{destination.company}</h2><p>{destination.address}</p><div className="miniGrid"><span>CNPJ: {destination.cnpj}</span><span>CEP: {destination.cep}</span><span>Recebe: {destination.hours}</span><span>Responsável: {destination.responsible}</span></div></div><div className="panel"><div className="sectionTitle"><Timer/>Próximas ações</div><div className="quickGrid"><button className="quick" onClick={()=>setScreen('quote')}><Send/><strong>Criar cotação</strong><span>Selecionar transportadoras e gerar fila</span></button><button className="quick" onClick={()=>setScreen('suppliers')}><Building2/><strong>Cadastrar fornecedor</strong><span>Preenchimento automático na próxima cotação</span></button><button className="quick" onClick={()=>setScreen('history')}><History/><strong>Ver pendências</strong><span>Lançar respostas e escolher melhor frete</span></button></div></div><div className="panel"><div className="sectionTitle"><History/>Últimas cotações</div>{!quotes.length?<Empty text="Nenhuma cotação criada ainda."/>:<RecentQuotes quotes={quotes.slice(0,5)}/>}</div></section>
}
function RecentQuotes({quotes}){return <div className="list">{quotes.map(q=><div className="listItem" key={q.id}><div><strong>{q.pickup?.name||'Sem remetente'}</strong><p>{q.cargo?.material||'-'} • {formatDate(q.created_at)}</p></div><span className={`status ${q.status}`}>{stageLabel(q.status)}</span></div>)}</div>}

function QuoteForm({destination,suppliers,carriers,setSuppliers,quotes,setQuotes,setScreen}){
 const [selectedSupplier,setSelectedSupplier]=useState(''); const [pickup,setPickup]=useState({name:'',document:'',cep:'',address:'',city:'',responsible:'',phone:'',hours:'',notes:''});
 const [cargo,setCargo]=useState({material:'',volumes:'1',weight:'',length:'',width:'',height:'',value:'',invoice:false,pallet:false,helper:false,munck:false,forkliftOrigin:false,forkliftGift:true,urgent:false,notes:''});
 const [selected,setSelected]=useState([]); const [preview,setPreview]=useState(null);
 const activeCarriers=carriers.filter(c=>c.active!==false);
 function chooseSupplier(id){ setSelectedSupplier(id); const s=suppliers.find(x=>x.id===id); if(s) setPickup({name:s.name,document:s.document,cep:s.cep,address:s.address,city:s.city,responsible:s.responsible,phone:s.phone,hours:s.hours,notes:s.notes}); }
 function toggleCarrier(id){ setSelected(prev=>prev.includes(id)?prev.filter(x=>x!==id):[...prev,id]); }
 function selectAll(){ setSelected(activeCarriers.map(c=>c.id)); }
 async function saveSupplierFromPickup(){ if(!pickup.name) return alert('Informe o nome do fornecedor/remetente.'); const exists=suppliers.find(s=>s.name.toLowerCase()===pickup.name.toLowerCase()); if(exists) return alert('Fornecedor já existe.'); await setSuppliers([{id:uid('sup'),created_at:new Date().toISOString(),active:true,name:pickup.name,document:pickup.document,cep:pickup.cep,address:pickup.address,city:pickup.city,responsible:pickup.responsible,phone:pickup.phone,hours:pickup.hours,notes:pickup.notes},...suppliers]); alert('Fornecedor salvo.'); }
 async function generate(){ if(!pickup.name || !cargo.material) return alert('Informe remetente e tipo de material.'); if(!selected.length) return alert('Selecione pelo menos uma transportadora.'); const selectedCarriers=activeCarriers.filter(c=>selected.includes(c.id)); const queue=selectedCarriers.map(c=>({id:uid('fila'),carrier_id:c.id,carrier:c.name,whatsapp:c.whatsapp||'', website:c.website||'', status: c.whatsapp?'pendente':'sem_whatsapp', opened_at:null, sent_at:null, notes:''})); const q={id:uid('quote'),created_at:new Date().toISOString(),status:'rascunho',destination,pickup,cargo:{...cargo,cubage:calcCubage(cargo)},sent_to:queue.map(x=>x.carrier),dispatch_queue:queue,responses:[],attachments:[]}; const rows=[q,...quotes]; await setQuotes(rows); setPreview(q); }
 return <section className="quoteLayout"><div className="panel"><div className="sectionTitle"><Building2/>Destino fixo</div><div className="fixedCard"><strong>{destination.company}</strong><p>{destination.address}</p><span>CNPJ/Pagador: {destination.cnpj} • Recebimento: {destination.hours} • Responsável: {destination.responsible}</span></div></div><div className="panel"><div className="sectionTitle"><Building2/>Coleta / remetente</div><div className="formGrid"><SelectField label="Fornecedor salvo" value={selectedSupplier} onChange={chooseSupplier}><option value="">Preencher manualmente</option>{suppliers.filter(s=>s.active!==false).map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</SelectField><Field label="Nome do remetente" value={pickup.name} onChange={v=>setPickup({...pickup,name:v})}/><Field label="CNPJ/CPF" value={pickup.document} onChange={v=>setPickup({...pickup,document:v})}/><Field label="CEP" value={pickup.cep} onChange={v=>setPickup({...pickup,cep:v})}/><Field label="Endereço de coleta" value={pickup.address} onChange={v=>setPickup({...pickup,address:v})}/><Field label="Cidade/UF" value={pickup.city} onChange={v=>setPickup({...pickup,city:v})}/><Field label="Responsável" value={pickup.responsible} onChange={v=>setPickup({...pickup,responsible:v})}/><Field label="WhatsApp/telefone" value={pickup.phone} onChange={v=>setPickup({...pickup,phone:v})}/><Field label="Horário de coleta" value={pickup.hours} onChange={v=>setPickup({...pickup,hours:v})}/><Field label="Observações do local" textarea value={pickup.notes} onChange={v=>setPickup({...pickup,notes:v})}/></div><button className="secondary" onClick={saveSupplierFromPickup}><Save size={16}/> Salvar como fornecedor</button></div><div className="panel"><div className="sectionTitle"><Package/>Carga completa</div><div className="formGrid"><Field label="Tipo de material" value={cargo.material} onChange={v=>setCargo({...cargo,material:v})} placeholder="Ex: peça industrial, máquina, motor..."/><Field label="Valor da mercadoria" value={cargo.value} onChange={v=>setCargo({...cargo,value:v})}/><Field label="Quantidade de volumes" value={cargo.volumes} onChange={v=>setCargo({...cargo,volumes:v})}/><Field label="Peso total kg" value={cargo.weight} onChange={v=>setCargo({...cargo,weight:v})}/><Field label="Comprimento cm" value={cargo.length} onChange={v=>setCargo({...cargo,length:v})}/><Field label="Largura cm" value={cargo.width} onChange={v=>setCargo({...cargo,width:v})}/><Field label="Altura cm" value={cargo.height} onChange={v=>setCargo({...cargo,height:v})}/><Field label="Observações da carga" textarea value={cargo.notes} onChange={v=>setCargo({...cargo,notes:v})}/></div><div className="switchGrid"><Toggle label="Nota fiscal emitida" checked={cargo.invoice} onChange={v=>setCargo({...cargo,invoice:v})}/><Toggle label="Paletizado" checked={cargo.pallet} onChange={v=>setCargo({...cargo,pallet:v})}/><Toggle label="Precisa ajudante" checked={cargo.helper} onChange={v=>setCargo({...cargo,helper:v})}/><Toggle label="Precisa munck" checked={cargo.munck} onChange={v=>setCargo({...cargo,munck:v})}/><Toggle label="Empilhadeira na origem" checked={cargo.forkliftOrigin} onChange={v=>setCargo({...cargo,forkliftOrigin:v})}/><Toggle label="Coleta urgente" checked={cargo.urgent} onChange={v=>setCargo({...cargo,urgent:v})}/></div><div className="cubage">Cubagem calculada: <strong>{calcCubage(cargo).toFixed(3)} m³</strong></div></div><div className="panel"><div className="sectionTitle"><Truck/>Selecionar transportadoras</div><div className="toolbar"><button className="secondary" onClick={selectAll}><CheckSquare size={16}/> Selecionar todas</button><button className="secondary" onClick={()=>setSelected([])}><Square size={16}/> Limpar</button><span className="hint">{selected.length} selecionada(s)</span></div><div className="carrierSelectGrid">{activeCarriers.map(c=><button key={c.id} className={`carrierSelect ${selected.includes(c.id)?'selected':''}`} onClick={()=>toggleCarrier(c.id)}><div><strong>{c.name}</strong><span>{c.whatsapp?`WhatsApp: ${c.whatsapp}`:'Sem WhatsApp confirmado'}</span></div>{selected.includes(c.id)?<CheckSquare/>:<Square/>}</button>)}</div><button className="primary big" onClick={generate}><Send size={18}/> Gerar fila de envio</button></div>{preview&&<QuoteResult quote={preview} carriers={carriers} quotes={quotes} setQuotes={setQuotes} setScreen={setScreen}/>}</section>
}
function buildMessage(q){ const d=q.destination, p=q.pickup, c=q.cargo; return `Olá, preciso de uma cotação de frete para coleta e entrega na GIFT EXCELLENCE.\n\nDADOS DO PAGADOR / DESTINO:\nEmpresa: ${d.company}\nCNPJ: ${d.cnpj}\nEndereço de entrega: ${d.address}\nCEP: ${d.cep}\nResponsável: ${d.responsible}\nTelefone: ${d.phone}\nHorário de recebimento: ${d.hours}\n\nDADOS DA COLETA:\nRemetente: ${p.name}\nCNPJ/CPF: ${p.document||'-'}\nEndereço de coleta: ${p.address||'-'}\nCidade/UF: ${p.city||'-'}\nResponsável: ${p.responsible||'-'}\nTelefone: ${p.phone||'-'}\nHorário disponível para coleta: ${p.hours||'-'}\n\nDADOS DA CARGA:\nTipo de material: ${c.material}\nQuantidade de volumes: ${c.volumes||'-'}\nPeso total: ${c.weight||'-'} kg\nMedidas: ${c.length||'-'} x ${c.width||'-'} x ${c.height||'-'} cm\nCubagem/metragem: ${(c.cubage||0).toFixed(3)} m³\nValor da mercadoria: ${brl(c.value)}\nNota fiscal emitida: ${c.invoice?'Sim':'Não'}\nPaletizado: ${c.pallet?'Sim':'Não'}\nPrecisa de ajudante: ${c.helper?'Sim':'Não'}\nPrecisa de munck: ${c.munck?'Sim':'Não'}\nEmpilhadeira na origem: ${c.forkliftOrigin?'Sim':'Não'}\nUrgente: ${c.urgent?'Sim':'Não'}\n\nObservações:\n${c.notes||p.notes||'-'}\n\nPode me passar valor do frete, prazo e previsão de coleta?`; }
function QuoteResult({quote,carriers,quotes,setQuotes,setScreen}){ async function update(q){ await setQuotes(quotes.map(x=>x.id===q.id?q:x)); }
 return <div className="panel result"><div className="sectionTitle"><MessageCircle/>Fila criada</div><QueueManager quote={quote} carriers={carriers} updateQuote={update}/><div className="messageBox"><div className="toolbar"><button className="secondary" onClick={()=>navigator.clipboard.writeText(buildMessage(quote))}><Copy size={16}/> Copiar mensagem</button><button className="secondary" onClick={()=>window.print()}><FileText size={16}/> PDF / Imprimir</button><button className="primary" onClick={()=>setScreen('history')}><History size={16}/> Ver no histórico</button></div><pre>{buildMessage(quote)}</pre></div></div> }
function QueueManager({quote,carriers,updateQuote}){ const [q,setQ]=useState(quote); useEffect(()=>setQ(quote),[quote]); const message=buildMessage(q); function patchQueue(id,patch){ const nq={...q,dispatch_queue:q.dispatch_queue.map(item=>item.id===id?{...item,...patch}:item)}; const sent=nq.dispatch_queue.filter(i=>i.status==='enviado').length; const responded=nq.dispatch_queue.filter(i=>i.status==='respondeu').length; nq.status = responded ? 'parcial' : sent ? 'enviada' : 'rascunho'; setQ(nq); updateQuote(nq); }
 function openWhats(item){ if(!item.whatsapp) return navigator.clipboard.writeText(message).then(()=>alert('Transportadora sem WhatsApp. Mensagem copiada.')); const url=`https://wa.me/55${onlyDigits(item.whatsapp)}?text=${encodeWhats(message)}`; window.open(url,'_blank'); patchQueue(item.id,{status:'aberto',opened_at:new Date().toISOString()}); }
 function openNext(){ const next=q.dispatch_queue.find(i=>['pendente','aberto'].includes(i.status) && i.whatsapp); if(!next) return alert('Não há próximo WhatsApp pendente com número confirmado.'); openWhats(next); }
 return <div><div className="queueHeader"><button className="primary" onClick={openNext}><ArrowRight size={17}/> Abrir próximo WhatsApp</button><span>{q.dispatch_queue.filter(i=>i.status==='enviado').length}/{q.dispatch_queue.length} marcadas como enviadas</span></div><div className="queueList">{q.dispatch_queue.map(item=><div className="queueItem" key={item.id}><div><strong>{item.carrier}</strong><p>{item.whatsapp?`WhatsApp: ${item.whatsapp}`:'Sem WhatsApp confirmado • use copiar mensagem/site'}</p><span className={`status ${item.status}`}>{item.status.replace('_',' ')}</span></div><div className="actions"><button className={item.whatsapp?'mini ok':'mini'} onClick={()=>openWhats(item)}>{item.whatsapp?<MessageCircle size={15}/>:<Copy size={15}/>} {item.whatsapp?'WhatsApp':'Copiar'}</button>{item.website&&<a className="mini" href={item.website} target="_blank"><ExternalLink size={15}/> Site</a>}<button className="mini" onClick={()=>patchQueue(item.id,{status:'enviado',sent_at:new Date().toISOString()})}>Enviado</button><button className="mini ok" onClick={()=>patchQueue(item.id,{status:'respondeu'})}>Respondeu</button><button className="mini off" onClick={()=>patchQueue(item.id,{status:'sem_retorno'})}>Sem retorno</button></div></div>)}</div></div> }

function CrudList({type,items,setItems,blank,fields,title,icon}){ const [form,setForm]=useState(blank); const [edit,setEdit]=useState(null); const [q,setQ]=useState(''); const filtered=items.filter(i=>(i.name||'').toLowerCase().includes(q.toLowerCase())); async function save(){ if(!form.name) return alert('Informe o nome.'); let rows=edit?items.map(i=>i.id===edit?{...i,...form}:i):[{...form,id:uid(type),created_at:new Date().toISOString(),active:form.active??true},...items]; await setItems(rows); setForm(blank); setEdit(null); } function start(i){setEdit(i.id); setForm({...blank,...i})} async function del(id){ if(confirm('Apagar este cadastro?')) await setItems(items.filter(i=>i.id!==id)); } async function toggle(i){ await setItems(items.map(x=>x.id===i.id?{...x,active:x.active===false?true:false}:x)); }
 return <section className="gridPage"><div className="panel"><div className="sectionTitle">{icon}{edit?'Editar':'Cadastrar'} {title}</div><div className="formGrid">{fields.map(f=><Field key={f.key} label={f.label} value={form[f.key]} textarea={f.textarea} onChange={v=>setForm({...form,[f.key]:v})}/>)}</div><div className="toolbar"><Toggle label="Ativo" checked={form.active!==false} onChange={v=>setForm({...form,active:v})}/><button className="primary" onClick={save}><Save size={16}/> Salvar</button>{edit&&<button className="secondary" onClick={()=>{setEdit(null);setForm(blank)}}><X size={16}/> Cancelar</button>}</div></div><div className="panel"><div className="sectionTitle"><Search/>Lista</div><label className="search"><Search size={16}/><input placeholder="Pesquisar..." value={q} onChange={e=>setQ(e.target.value)}/></label>{!filtered.length?<Empty text="Nada cadastrado."/>:<div className="list">{filtered.map(i=><div className="listItem" key={i.id}><div><strong>{i.name}</strong><p>{i.city||i.address||i.cargo_type||'-'}</p><small>{i.notes||i.quote_mode||''}</small></div><div className="actions"><button className={`mini ${i.active!==false?'ok':'off'}`} onClick={()=>toggle(i)}>{i.active!==false?'Ativo':'Inativo'}</button><button className="iconBtn" onClick={()=>start(i)}><Pencil size={16}/></button><button className="iconBtn danger" onClick={()=>del(i.id)}><Trash2 size={16}/></button></div></div>)}</div>}</div></section> }
function Suppliers({suppliers,setSuppliers}){return <CrudList type="sup" items={suppliers} setItems={setSuppliers} title="Fornecedores / locais de coleta" icon={<Building2/>} blank={{name:'',document:'',cep:'',address:'',city:'',responsible:'',phone:'',hours:'',notes:'',active:true}} fields={[{key:'name',label:'Nome da empresa/remetente'},{key:'document',label:'CNPJ/CPF'},{key:'cep',label:'CEP'},{key:'address',label:'Endereço completo'},{key:'city',label:'Cidade/UF'},{key:'responsible',label:'Responsável'},{key:'phone',label:'WhatsApp/telefone'},{key:'hours',label:'Horário de coleta'},{key:'notes',label:'Observações',textarea:true}]}/>} 
function Carriers({carriers,setCarriers}){return <CrudList type="car" items={carriers} setItems={setCarriers} title="Transportadoras" icon={<Truck/>} blank={{name:'',whatsapp:'',email:'',city:'',cargo_type:'',quote_mode:'Manual / WhatsApp',api_status:'API futura',website:'',notes:'',active:true}} fields={[{key:'name',label:'Nome da transportadora'},{key:'whatsapp',label:'WhatsApp confirmado'},{key:'email',label:'E-mail'},{key:'city',label:'Cidade/UF'},{key:'cargo_type',label:'Tipo de carga atendida'},{key:'quote_mode',label:'Modo de cotação'},{key:'api_status',label:'Status da API'},{key:'website',label:'Site/portal'},{key:'notes',label:'Observações',textarea:true}]}/>} 
function HistoryPage({quotes,setQuotes,carriers}){ const [open,setOpen]=useState(null); const [filter,setFilter]=useState(''); const list=quotes.filter(q=>`${q.pickup?.name} ${q.cargo?.material} ${q.status}`.toLowerCase().includes(filter.toLowerCase())); async function updateQuote(q){ await setQuotes(quotes.map(x=>x.id===q.id?q:x)); setOpen(q); } async function del(id){ if(confirm('Apagar cotação do histórico?')) await setQuotes(quotes.filter(q=>q.id!==id)); } async function duplicate(q){ const nq={...q,id:uid('quote'),created_at:new Date().toISOString(),status:'rascunho',dispatch_queue:(q.dispatch_queue||[]).map(i=>({...i,id:uid('fila'),status:i.whatsapp?'pendente':'sem_whatsapp',opened_at:null,sent_at:null})),responses:[]}; await setQuotes([nq,...quotes]); setOpen(nq); }
 function downloadCSV(){ const rows=[['Data','Fornecedor','Cidade','Material','Valor mercadoria','Status','Melhor valor','Menor prazo','Escolhida'],...quotes.map(q=>[formatDate(q.created_at),q.pickup?.name,q.pickup?.city,q.cargo?.material,brl(q.cargo?.value),stageLabel(q.status),bestValue(q.responses),bestDeadline(q.responses),chosen(q.responses)])]; const csv=rows.map(r=>r.map(c=>`"${String(c??'').replaceAll('"','""')}"`).join(';')).join('\n'); const blob=new Blob([csv],{type:'text/csv;charset=utf-8'}); const a=document.createElement('a'); a.href=URL.createObjectURL(blob); a.download='historico-gift-log.csv'; a.click(); }
 return <section className="gridPage"><div className="panel"><div className="toolbar between"><div className="sectionTitle"><History/>Histórico de cotações</div><button className="secondary" onClick={downloadCSV}><Download size={16}/> CSV</button></div><label className="search"><Search size={16}/><input placeholder="Pesquisar por fornecedor, material ou status..." value={filter} onChange={e=>setFilter(e.target.value)}/></label>{!list.length?<Empty text="Nenhuma cotação encontrada."/>:<div className="tableWrap"><table><thead><tr><th>Data</th><th>Fornecedor</th><th>Material</th><th>Status</th><th>Fila</th><th>Melhor valor</th><th>Ações</th></tr></thead><tbody>{list.map(q=><tr key={q.id}><td>{formatDate(q.created_at)}</td><td>{q.pickup?.name}</td><td>{q.cargo?.material}</td><td><span className={`status ${q.status}`}>{stageLabel(q.status)}</span></td><td>{q.dispatch_queue?.length||0} transp.</td><td>{bestValue(q.responses)}</td><td><div className="actions"><button className="mini" onClick={()=>setOpen(q)}>Abrir</button><button className="mini" onClick={()=>duplicate(q)}><RotateCcw size={14}/> Duplicar</button><button className="iconBtn danger" onClick={()=>del(q.id)}><Trash2 size={15}/></button></div></td></tr>)}</tbody></table></div>}</div>{open&&<QuoteDetail q={open} setOpen={setOpen} carriers={carriers} updateQuote={updateQuote}/>}</section> }
function bestValue(responses=[]){ const vals=responses.map(r=>Number(String(r.value||'').replace(',','.'))).filter(Boolean); return vals.length?brl(Math.min(...vals)):'-'; }
function bestDeadline(responses=[]){ const r=responses.filter(x=>x.deadline).sort((a,b)=>Number(a.deadline)-Number(b.deadline))[0]; return r?.deadline||'-'; }
function chosen(responses=[]){ return responses.find(r=>r.chosen)?.carrier||'-'; }
function QuoteDetail({q,setOpen,carriers,updateQuote}){ const [res,setRes]=useState({carrier:'',value:'',deadline:'',pickupForecast:'',contact:'',notes:'',chosen:false}); const [fileName,setFileName]=useState(''); async function add(){ if(!res.carrier) return alert('Informe a transportadora.'); const nq={...q,responses:[...(q.responses||[]),{...res,id:uid('res'),created_at:new Date().toISOString()}],status:'respondido'}; await updateQuote(nq); setRes({carrier:'',value:'',deadline:'',pickupForecast:'',contact:'',notes:'',chosen:false}); } async function choose(id){ const nq={...q,status:'fechado',responses:q.responses.map(r=>({...r,chosen:r.id===id}))}; await updateQuote(nq); } async function addAttachment(){ if(!fileName) return; const nq={...q,attachments:[...(q.attachments||[]),{id:uid('att'),name:fileName,created_at:new Date().toISOString()}]}; await updateQuote(nq); setFileName(''); }
 const best=bestValue(q.responses); return <div className="panel detail"><button className="close" onClick={()=>setOpen(null)}><X/></button><div className="sectionTitle"><FileText/>Cotação detalhada</div><div className="summaryGrid"><Stat label="Melhor valor" value={best}/><Stat label="Menor prazo" value={bestDeadline(q.responses)}/><Stat label="Escolhida" value={chosen(q.responses)}/></div><div className="messageBox"><div className="toolbar"><button className="secondary" onClick={()=>navigator.clipboard.writeText(buildMessage(q))}><Copy size={16}/> Copiar mensagem</button><button className="secondary" onClick={()=>window.print()}><FileText size={16}/> PDF / Imprimir</button></div><pre>{buildMessage(q)}</pre></div><QueueManager quote={q} carriers={carriers} updateQuote={updateQuote}/><div className="sectionTitle"><DollarSign/>Lançar resposta</div><div className="formGrid"><SelectField label="Transportadora" value={res.carrier} onChange={v=>setRes({...res,carrier:v})}><option value="">Selecione</option>{(q.dispatch_queue||[]).map(c=><option key={c.id} value={c.carrier}>{c.carrier}</option>)}</SelectField><Field label="Valor do frete" value={res.value} onChange={v=>setRes({...res,value:v})}/><Field label="Prazo" value={res.deadline} onChange={v=>setRes({...res,deadline:v})} placeholder="Ex: 2 dias"/><Field label="Previsão de coleta" value={res.pickupForecast} onChange={v=>setRes({...res,pickupForecast:v})}/><Field label="Quem respondeu" value={res.contact} onChange={v=>setRes({...res,contact:v})}/><Field label="Observações" textarea value={res.notes} onChange={v=>setRes({...res,notes:v})}/></div><button className="primary" onClick={add}><Plus size={16}/> Adicionar resposta</button><div className="sectionTitle"><Star/>Comparador</div>{(!q.responses||!q.responses.length)?<Empty text="Nenhuma resposta lançada."/>:<div className="list">{q.responses.map(r=><div className={`listItem ${r.chosen?'chosen':''}`} key={r.id}><div><strong>{r.carrier} {r.chosen&&<CheckCircle2 size={15}/>}</strong><p>{brl(r.value)} • Prazo: {r.deadline||'-'} • Coleta: {r.pickupForecast||'-'}</p><small>{r.contact?`Contato: ${r.contact} • `:''}{r.notes}</small></div><button className="mini ok" onClick={()=>choose(r.id)}>Escolher</button></div>)}</div>}<div className="sectionTitle"><Upload/>Anexos simples</div><div className="toolbar"><Field label="Nome do anexo/comprovante" value={fileName} onChange={setFileName} placeholder="Ex: print Braspress 22-06"/><button className="secondary" onClick={addAttachment}><Plus size={16}/> Adicionar</button></div>{(q.attachments||[]).length>0&&<div className="chips">{q.attachments.map(a=><span key={a.id}>{a.name}</span>)}</div>}</div> }
function SettingsPage({destination,setDestination}){function resetCarriers(){ if(confirm('Restaurar lista padrão de transportadoras? Isso não apaga fornecedores nem histórico.')){ safeWrite(STORAGE_KEYS.carriers,DEFAULT_CARRIERS); location.reload(); }} return <section className="gridPage"><div className="panel"><div className="sectionTitle"><Settings/>Destino fixo / dados da GIFT</div><div className="formGrid"><Field label="Empresa" value={destination.company} onChange={v=>setDestination({...destination,company:v})}/><Field label="CNPJ / Pagador" value={destination.cnpj} onChange={v=>setDestination({...destination,cnpj:v})}/><Field label="CEP" value={destination.cep} onChange={v=>setDestination({...destination,cep:v})}/><Field label="Endereço" value={destination.address} onChange={v=>setDestination({...destination,address:v})}/><Field label="Telefone" value={destination.phone} onChange={v=>setDestination({...destination,phone:v})}/><Field label="Responsável" value={destination.responsible} onChange={v=>setDestination({...destination,responsible:v})}/><Field label="Horário de recebimento" value={destination.hours} onChange={v=>setDestination({...destination,hours:v})}/></div><p className="hint">Esses dados entram automaticamente em toda cotação.</p></div><div className="panel"><div className="sectionTitle"><Database/>Supabase ready</div><p>Configure na Vercel:</p><pre className="code">VITE_SUPABASE_URL=...\nVITE_SUPABASE_ANON_KEY=...</pre><p>Depois rode o SQL atualizado incluído na pasta <strong>/supabase/schema.sql</strong>.</p><button className="secondary" onClick={resetCarriers}><Truck size={16}/> Restaurar transportadoras padrão</button></div></section>}
