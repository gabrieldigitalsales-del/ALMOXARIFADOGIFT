export function Field({label,value,onChange,type='text',options,textarea,disabled,placeholder,min,step}){return <label className="grid gap-1 text-sm font-semibold"><span>{label}</span>{textarea?<textarea rows={3} className="input" value={value??''} disabled={disabled} placeholder={placeholder} onChange={e=>onChange?.(e.target.value)}/>:options?<select className="input" value={value??''} disabled={disabled} onChange={e=>onChange?.(e.target.value)}>{options.map(o=><option key={typeof o==='object'?o.value:o} value={typeof o==='object'?o.value:o}>{typeof o==='object'?o.label:o}</option>)}</select>:<input className="input" type={type} min={min} step={step} value={value??''} disabled={disabled} placeholder={placeholder} onChange={e=>onChange?.(type==='number'?Number(e.target.value):e.target.value)}/>}</label>}
export default function FormGrid({children}){return <div className="grid gap-4 md:grid-cols-2">{children}</div>}
