import{Edit,Trash2,Eye}from'lucide-react';
export default function DataTable({columns,rows,onEdit,onDelete,onView,empty='Nenhum registro encontrado'}){return <div className="overflow-hidden rounded-none border border-brand-line bg-white dark:border-white/10 dark:bg-brand-graphite"><div className="overflow-x-auto"><table className="min-w-full"><thead><tr>{columns.map(c=><th key={c.key}>{c.label}</th>)}{(onView||onEdit||onDelete)&&<th>Ações</th>}</tr></thead><tbody>{rows.length?rows.map((r,i)=><tr key={r.id||i} className="hover:bg-brand-turquoise/10 dark:hover:bg-white/5">{columns.map(c=><td key={c.key}>{c.render?c.render(r,i):r[c.key]}</td>)}{(onView||onEdit||onDelete)&&<td><div className="flex gap-2">{onView&&<button onClick={()=>onView(r)} className="btn-ghost p-2"><Eye size={16}/></button>}{onEdit&&<button onClick={()=>onEdit(r)} className="btn-ghost p-2"><Edit size={16}/></button>}{onDelete&&<button onClick={()=>onDelete(r)} className="btn-ghost p-2 text-brand-red"><Trash2 size={16}/></button>}</div></td>}</tr>):<tr><td colSpan={columns.length+1} className="py-10 text-center text-brand-steel">{empty}</td></tr>}</tbody></table></div></div>}
