import{useEffect,useState}from'react';import{AlertTriangle,X}from'lucide-react';
export default function Modal({open,title,children,onClose,size='max-w-3xl',dirty=false,confirmMessage='Você tem informações digitadas nessa janela. Deseja sair sem salvar?'}){const[confirmClose,setConfirmClose]=useState(false);useEffect(()=>{if(!open){setConfirmClose(false);return}const onKey=e=>{if(e.key==='Escape')tryClose()};window.addEventListener('keydown',onKey);return()=>window.removeEventListener('keydown',onKey)},[open,dirty]);if(!open)return null;const closeNow=()=>{setConfirmClose(false);onClose?.()};const tryClose=()=>{if(dirty){setConfirmClose(true);return}closeNow()};return <div className="fixed inset-0 z-50 grid place-items-center bg-black/70 p-4" onMouseDown={e=>{if(e.target===e.currentTarget)tryClose()}}><div className={`max-h-[92vh] w-full ${size} overflow-y-auto rounded-none bg-white p-6 shadow-industrial dark:bg-brand-graphite`} onMouseDown={e=>e.stopPropagation()}><div className="mb-4 flex items-center justify-between border-b border-brand-line pb-3 dark:border-white/10"><h3 className="text-xl font-semibold">{title}</h3><button onClick={tryClose} className="btn-ghost p-2"><X/></button></div>{children}</div>{confirmClose&&<div className="fixed inset-0 z-[60] grid place-items-center bg-black/75 p-4" onMouseDown={e=>e.stopPropagation()}><div className="w-full max-w-md border border-brand-line bg-white p-6 shadow-industrial dark:border-white/10 dark:bg-[#15191b]"><div className="mb-4 flex items-start gap-3"><div className="grid h-11 w-11 shrink-0 place-items-center border border-brand-yellow bg-brand-yellow/20 text-brand-yellow"><AlertTriangle size={22}/></div><div><h3 className="text-xl font-semibold">Alterações não salvas</h3><p className="mt-1 text-sm text-brand-steel dark:text-white/60">{confirmMessage}</p></div></div><div className="mt-5 flex flex-wrap justify-end gap-2"><button className="btn-ghost" onClick={()=>setConfirmClose(false)}>Continuar editando</button><button className="btn-danger" onClick={closeNow}>Sair sem salvar</button></div></div></div>}</div>}
